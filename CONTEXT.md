<!-- pg-core 术语表（权威）。母本为 PackGradle 仓库根 CONTEXT.md（Go 时代），随 pg-core 决策图（skyraah/PackGradle#114）各票收口演化。 -->
# PackGradle

Minecraft 模组包项目源（Packwiz）与运行实例（Prism）之间受管同步的桌面工具。本文件是全项目唯一术语表。

## Language

**工作区（Workspace）**:
一个项目源与一个运行实例之间受管同步关系的唯一产品投影，对应一条 Relation。
_Avoid_: 项目联动、链接（旧 dir link 语境）

**项目源（Project）**:
以 Packwiz `pack.toml` 为根的模组包项目端点，同步的一方。
_Avoid_: 项目（指端点时）、pack

**运行实例（Runtime）**:
以 Prism 实例（`<实例>/minecraft`）为根的运行时端点，同步的另一方。
_Avoid_: 实例、Prism 联动

**修订号（Revision）**:
一条 Relation 的策略代次；创建时即第 1 代，仅随 MappingPolicy 修改递增，不在 UI 展示（内部一致性字段）；作为多客户端进程经 SQLite 共享数据库写入时的并发校验。见 ADR-0002、ADR-0019。
_Avoid_: 版本（指关系时）、策略集版本

**策略集版本（Policy Set Version）**:
MappingPolicy 模板自身的版本（如 default-v1 的 1），随模板演进变化；与关系修订号无关。
_Avoid_: 修订号（指模板时）

**同步范围（Sync Scope）**:
一条关系受同步管理的资源集合，由映射策略显式声明（目录、文件与忽略规则）；范围外的资源仍可能进扫描快照，但不出现在差异、计划与快速更新中，直至规则改回。见 ADR-0013。
_Avoid_: 受管范围（旧称，2026-09-10 更名）、选择性同步（旧栈 REQ-3.6 口径）

**能力（Feature）**:
当前版本/平台实际实现的功能开关；`feature=false` 的动作不注册，前端不渲染入口。见 `docs/contract/03-p1-contract.md` §2.1。
_Avoid_: 功能（指单动作时）

**可用性（Availability）**:
单动作在当前工作区状态下的可执行性，由后端推导并携带原因码，前端不得自行推断。见 `docs/contract/03-p1-contract.md` §2.1。
_Avoid_: 权限、是否可点

**重绑（Rebind）**:
把关系一端的根路径替换为新端点（Prepare 预检 + Apply 执行）；Apply 后不继承基线、恒重走初始化；修订号不递增，旧计划因绑定指纹失配失效。见 ADR-0003、ADR-0019。
_Avoid_: 重新绑定（指 UI 文案时）

**删除工作区（Delete Workspace）**:
删除一条 Relation 的终态操作：前置为无活跃任务且不在恢复所需；删除关系及其计划/任务/预检等关系级依附状态；仍被其他关系引用的端点保留，失去全部引用的端点同事务清理；历史提交与 CAS 对象的处置归历史域。无软删除、不可恢复。见 ADR-0019。
_Avoid_: 解绑（指重绑语境）、归档

**扫描（Scan）**:
读取工作区双端管辖目录的当前文件状态——文件树、packwiz metafile 解析与内容摘要——产出两端各自的扫描快照；是差异计算的输入。
_Avoid_: 索引（指 .index 语境）、遍历（指实现方式）

**扫描快照（Scan Snapshot）**:
单端（项目源或运行实例）一次扫描产出的完整文件状态记录，带逐资源内容摘要；差异取两端最新快照与基线做三方比较。不被存活同步提交或计划引用、且不是该端最新一份的快照即孤儿快照，随垃圾回收清除。
_Avoid_: 基线（指提交后状态时）、缓存（指前端缓存时）

**差异（Diff）**:
以基线为公共祖先、项目源与运行实例最新扫描快照为输入的三方比较，逐资源判定变更来源与冲突；只有同步范围内的资源参与。是计划的输入。
_Avoid_: 冲突（指判定结果时）、变更（指前端页面语境时）

**计划（Plan）**:
差异经映射策略与冲突决议生成的有序操作集合（复制、合并、删除、下载物化等），携带确认要求、风险前置条件与有效期；Apply 运行只执行受确认的计划。
_Avoid_: 预案、任务列表

**上游变更（Upstream Change）**:
项目源侧由外部写者（如 git 拉取、packwiz 版本决策）造成的变更；同步只负责察觉并纳入三方差异，不追溯写者。
_Avoid_: 自动更新（指该语境时）、外部修改

**快速更新（Quick Update）**:
察觉上游变更后的一键同步入口：一次授权批量执行全部非冲突操作，冲突操作转为待确认计划。
_Avoid_: 一键更新、自动同步

**授权模式（Authorized Mode）**:
工作区级授权开关：开启后非冲突操作的执行免逐次确认；冲突与删除操作永不适用，恢复所需期间暂停生效（开关保留）。
_Avoid_: 自动模式、全自动、免确认（泛指时）

**受控重查（Controlled Re-query）**:
前端缓存失效后的唯一恢复通路：立即发起、进行中只标脏、结束后补刷一轮直至干净的全量重读管线；启动 bootstrap、漏包、失效事件、周期对账共用同一管线。见 `docs/contract/04-p1-event-protocol.md` §2.4。
_Avoid_: 刷新、轮询（指事件恢复时）

**事件流序号（Stream Sequence）**:
后端事件流的持久化单调序号，仅用于前端检测漏包；与任务内序号、关系修订号互不相干。见 `docs/contract/04-p1-event-protocol.md` §2.2。
_Avoid_: 序号（泛指时）、版本

**预检（Preparation）**:
创建或重绑关系前两段式（Prepare → Apply）的中间状态：携带端点草稿、初始策略与检查项，限时有效、仅可消费一次；执行失败回滚后同一预检可重试直至过期。见 ADR-0003。
_Avoid_: 预览、草稿（指端点登记时）

**Apply 运行（Apply Run）**:
一次受确认计划驱动的跨端同步执行，从准备、暂存、应用、验证到提交或进入恢复所需的完整运行事实；与前端任务进度短语分离。

**操作日志（Operation Journal）**:
Apply 运行中按有序操作记录意图、前后内容摘要、恢复引用和所有权证明的事实记录；当前状态可查询，状态变化保留追加历史。

**恢复探测（Recovery Probe）**:
恢复器对实际路径、before/after 摘要、临时内容和所有权证明进行的证据检查，用于决定标记完成、幂等重做、补偿或要求人工确认；不凭外观猜测。

**暂存（Staging）**:
Apply 期间与单次运行隔离的临时内容和恢复证据区域；成功提交后才可按所有权证明清理，恢复所需时必须保留。

**恢复所需（Recovery Required）**:
Apply 未能证明已安全完成或回滚的事实状态；对应工作区禁止新的 Apply 与回滚，直到恢复探测得到可验证裁决或人工确认。

**所有权证明（Ownership Proof）**:
Apply 运行对「某处改动确属本次 Apply」签发的证据：由运行密钥签发、与操作行字段互验、与暂存内容/恢复引用同源；证明无效一律判含糊，绝不凭文件名、mtime 或外观猜测归属。普通用户视图不透出证明本身。
_Avoid_: 数字签名、指纹（指证明时）

**回滚（Restore）**:
以历史提交的结果基线为目标生成新计划并走完整确认与应用流程，把双端写回该提交完成后的状态；产生新提交、不改写历史；非 mod 内容取自对象库，mod 走远端重取，取不到则降级用户提供或标记不可恢复。恢复所需期间禁止发起。
_Avoid_: 恢复（专指 Apply 崩溃恢复语境）、还原、重置（指数据库时）

**同步提交（Sync Commit）**:
一次 Apply 完整收口后落库的不可变历史事实：exact/partial 完整性、剩余差异数与逐资源前后表示摘要；由验证快照与提交事务共同产生，恢复路径不产生（人工确认不推进基线、不建提交）。历史页展示的就是同步提交。
_Avoid_: 提交（指 git 语境时）、版本（指同步事实时）

**保留窗口（Retention Window）**:
同步提交按保留策略（数量/天数/容量，最近 3 个保底不裁）仍可回滚的范围；回滚可用窗口即保留窗口的函数。超窗提交随策略清理，属显式声明的删除，不是历史改写。见 ADR-0007。
_Avoid_: 备份策略、历史保存期、过期（指计划时）

**垃圾回收（Garbage Collection）**:
以后台任务形式回收已无存活引用的 CAS 对象的机制；只在无活跃写入运行且无恢复所需时执行，先修剪提交再收孤儿。见 ADR-0007。
_Avoid_: 清理缓存、GC（指编程语言时）、删文件（泛指时）

**孤儿对象（Orphan Object）**:
不被任何存活引用——存活提交、恢复引用、未提交暂存——指向的 CAS 对象或写中断残渣；垃圾回收的唯一合法对象。见 ADR-0007。
_Avoid_: 死对象、垃圾（泛指时）

**回收站（Trash）**:
垃圾回收删除 CAS 对象前的缓冲区：对象压缩移入、保留 7 天后物理清除，期间可人工复活；账目即对象行的隔离态。
_Avoid_: 暂存（专指 Staging）、隔离区、回收桶

**免钥匙直链（Keyless Direct Link）**:
由 metafile 自带的文件名与 CF 文件编号按固定规则拼出、直接从 CF 文件服务器（mediafilez.forgecdn.net）取字节、全程不经 CF API 不需钥匙的下载通道。v1 下载物化的唯一通道；校验失败或不可达即降级用户自备，不做无校验下载。见 ADR-0008。
_Avoid_: CDN 直链（泛指时）、破解（贬义误读）、API 下载

**下载物化（Download Materialization）**:
物化模式之一：运行端缺失的 mod 字节经免钥匙直链取得并走暂存→Apply 既有管线落盘；与 copy（本地取数）同为取数来源，取数失败按来源报告并由重新快速更新闭环消化。见 ADR-0008 与契约 06 §3.7。
_Avoid_: 下载器（指整条链路时）、自动下载

**合并（Merge）**:
对双侧都改过的文本资源，以基线为公共祖先做三方文本合并、产出保留双方改动的提议内容；干净合并（零冲突块且通过类型校验）计为非冲突操作并随授权模式免确认，含冲突块则按冲突处理。合并产物是本地合成内容，一律经对象库保全。见 ADR-0009；pg-core 全套维持定稿见 ADR-0020。
_Avoid_: 同步（指该语境时）、三方合并（指引擎内部时）、智能合并

**冲突块（Conflict Hunk）**:
单个文件内一处「双侧改了同一区域」的连续片段，整文件冲突卡片内的只读展示单元；决议粒度是整文件取侧，不做逐块选择，两全需求走手动兜底。见 ADR-0009；pg-core 维持定稿见 ADR-0020。
_Avoid_: 冲突（指整文件时）、merge 冲突

**监听（Watch）**:
应用运行期间对工作区双端管辖目录的常驻文件监视；变化经静默期聚合后触发自动快速更新，不可用时降级为手动快速更新并一次性提示。见 ADR-0010。
_Avoid_: 文件系统钩子、轮询（指触发方式时）、mods 监听（legacy 语境）

**自动快速更新（Auto Quick Update）**:
监听触发的快速更新：与手动入口同一条后端编排链、同一个免确认口径（确认要求为空且授权模式开启才自动执行），冲突与删除永不自动；连续失败达阈值即暂停该关系的自动面，手动快速更新成功后恢复。见 ADR-0010。
_Avoid_: 自动同步、后台同步、自动物化（物化专指取数来源）

**会话日志（Session Log）**:
每次应用启动产生一份、按启动时间戳分目录存放的结构化日志文件；常规保留最近 20 个会话（最近 3 份明文、更早压缩），100MB 总量硬顶优先于份数，超顶从最旧删起。见 ADR-0011。
_Avoid_: 滚动日志（按大小轮转语境）、日志文件（泛指时）

**别名路径（Aliased Path）**:
以端点角色前缀（`<project>/…`、`<runtime>/…`）表达的路径形式，是诊断详情与错误信息的存储默认形态；端点根之外的绝对路径不进诊断面。见 ADR-0011。
_Avoid_: 相对路径（指文件系统语义时）、脱敏路径（泛指时）

**孤儿快照（Orphan Snapshot）**:
不被任何存活同步提交或计划引用、且不是任一端最新一份的扫描快照；等同其版本无法被回滚，随垃圾回收清扫删除，保留窗口与同步提交共用同一套。见 ADR-0011。
_Avoid_: 过期快照（「过期」指计划语境）、历史快照（历史页展示的是同步提交）、孤儿对象（专指 CAS）

**忽略（Ignore）**:
把某资源移出同步管理的决定——生效后该资源不再出现在差异、计划与快速更新中，直至在同步范围页显式改回。冲突决议中的忽略随同步提交生效并落地为同步范围规则。见 ADR-0013。
_Avoid_: 跳过（指旧的一次性语义）、选择性同步（旧栈 REQ-3.6 口径）

**手动处理（Manual）**:
冲突决议之一——本次由用户在工具外自行处置，工具不生成操作，双端现状随提交吸收为新的基准；此后任一侧再变更照常回到差异面。见 ADR-0013。
_Avoid_: 跳过（语义已由忽略承接）

**凭据（Credential）**:
一条具名短秘密——CurseForge 钥匙、GitHub token、SSH 私钥口令等——由后端统一保管，明文只在后端解封与使用；前端只能写入、删除与查看设置状态。SSH 私钥文件本体不是凭据，其路径属普通配置。见 ADR-0015。
_Avoid_: 密钥（「运行密钥」专指 Apply 证明的 HMAC）、token（泛指时）

**凭据槽位（Credential Slot）**:
凭据的具名单位（如 `curseforge/api-key`）；新增一种凭据即新增一个槽位，存储与加密机制不变。见 ADR-0015。
_Avoid_: 条目、字段（指 config.toml 字段时）
